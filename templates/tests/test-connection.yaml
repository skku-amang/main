apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amang-web.fullname" . }}-test-connection"
  labels:
    {{- include "amang-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amang-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
