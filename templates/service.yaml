apiVersion: v1
kind: Service
metadata:
  name: {{ include "amang-web.fullname" . }}
  labels:
    {{- include "amang-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amang-web.selectorLabels" . | nindent 4 }}
